## .env 模板（共享、跨机器一致）
##
## 真实 .env 不进 git，由 install.sh 通过 iCloud Drive 同步到所有 Mac
## （真身在 ~/Library/Mobile Documents/com~apple~CloudDocs/.dotfiles/.env）
## 详见 README「.env 跨机同步」
##
## 适合放：
##   - 跨机器共享的凭据（GitHub Token、阿里云 AK 等）
##   - 跨机器一致的默认值
##
## 不适合放：
##   - 机器特定的值（机器名、本机独有的 token）→ 放 .env.local
##   - 生产 root 密码 / DB admin 密码等核弹级凭据 → 用 1Password
##
## 首次新建：cp .env.example .env

## 业务/凭据示例
FIRST_MSG=hello

## 机器名默认值（如需各机器不同，请在 .env.local 里覆盖）
COMPUTER_NAME=your-computer-name
