## .env 模板（共享、跨机器一致）
##
## 真实 .env 不进 git，由 install.sh 通过 vault 目录跨机器同步
## 详见 README「跨机同步：vault 设计」
##
## ─── 这个文件适合放什么 ────────────────────────────────────────────
##   ✓ 跨机器共享的凭据（GitHub Token、阿里云 AK 等）
##   ✓ 跨机器一致的默认值
##
##   ✗ 机器特定的值（机器名、本机独有的 token）→ 放 .env.local
##   ✗ 生产 root 密码 / DB admin 密码等核弹级凭据 → 用 1Password
##   ✗ DOTFILES_VAULT（见下方说明）
##
## ─── vault 后端怎么选 ─────────────────────────────────────────────
## 默认 vault = iCloud Drive：
##   ~/Library/Mobile Documents/com~apple~CloudDocs/.dotfiles/.env
##
## 想换成其他云盘 / 本地目录，把下面任意一行 export 加到
## ~/.zprofile 或 ~/.zshenv（不要放本文件！原因见下）：
##
##   export DOTFILES_VAULT="$HOME/Dropbox/.dotfiles"        # Dropbox
##   export DOTFILES_VAULT="$HOME/Sync/.dotfiles"           # Syncthing
##   export DOTFILES_VAULT="$HOME/OneDrive/.dotfiles"       # OneDrive
##   export DOTFILES_VAULT="$HOME/GoogleDrive/.dotfiles"    # Google Drive
##   export DOTFILES_VAULT="$HOME/.dotfiles-vault"          # 纯本地不同步
##
## ⚠️ 为什么 DOTFILES_VAULT 不能写在这个文件里：
##   .env 本身就放在 vault 里，install.sh 是先做 vault sync、再加载 .env。
##   如果把 vault 路径写在 .env，等 install.sh 想读它时还没加载，鸡生蛋蛋生鸡。
##   所以 DOTFILES_VAULT 必须从 shell 启动文件提前 export 进环境。
##
## ─── 首次新建 ────────────────────────────────────────────────────
##   cp .env.example .env

## 业务/凭据示例
FIRST_MSG=hello

## 机器名默认值（如需各机器不同，请在 .env.local 里覆盖）
COMPUTER_NAME=your-computer-name
